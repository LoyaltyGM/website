import * as React from "react";
import type { NextPage } from "next";
import Companies from "./companies";

const Home: NextPage = () => {
    return (
        <div>
            <Companies />
        </div>
    );
};

export default Home;
