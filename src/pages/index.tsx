import * as React from "react";
import type { NextPage } from "next";
import CreateNFT from "./сreate-nft";

const Home: NextPage = () => {
    return (
        <div>
            <CreateNFT />
        </div>
    );
};

export default Home;
