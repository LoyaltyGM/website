import * as React from "react";
import type { NextPage } from "next";
import CreateNFT from "./CreateNFT";

const Home: NextPage = () => {
    return (
        <div>
            <CreateNFT />
        </div>
    );
};

export default Home;
